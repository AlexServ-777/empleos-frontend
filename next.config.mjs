/** @type {import('next').NextConfig} */
const nextConfig = {
    /*async rewrites(){
    return [
      {
        source:'/back/:path*',
        destination:'https://localhost:8000/:path*',
      }
    ]
  },*/
};

export default nextConfig;
